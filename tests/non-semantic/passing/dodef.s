<-
    Verify that do/while loop definition syntax
    is accepted.
->

class cname() {

    func fname()() {

        do {} while(not x equ 123);
        do {} while(true);
        do {} while(compound.name lt 10);
    }
}
#******************
*
*    This is a cursory test for expressions to check the
*    syntax is being accepted.
*
*******************-**-******#

some_class_name {

    number xyz = asd.dfg;
    number asdf = 123.123 + 234 * 85;
    bool qwe = (qwe.we >=  wer) and (123.2 > 1.0);
    bool qwe = string:(qwe.we <= wer) and (123 > 1);
    bool qwe = bool:((qwe.we != wer) and (123.4 > 1));
    bool qwe = ((qwe.we > wer) and (bool:123 gt 1.2));

    some_func_name()() {
        # var = var1[1+2*3];
        var = "some string":(ver, 0.1+0.2*0.3, qwe.erw);
        var = 10.0/2.5+1.5;
    }
}

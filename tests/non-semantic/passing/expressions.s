#*
    This is a cursory test for expressions to check the
    syntax is being accepted.
*#

some_class_name {

    number xyz = asd.dfg;
    number asdf = 123.123 + 234.0 * 85.1;
    bool qwe = (qwe.we ==  wer) and (123.2 > 1.0);
    bool qwe = string:(qwe.we equ wer) and (123.3 gt 1.1);
    bool qwe = bool:((qwe.we equ wer) and (123.4 gt 1.3));
    bool qwe = ((qwe.we equ wer) and (bool:123.5 gt 1.2));

    some_func_name()() {
        # var = var1[1+2*3];
        var = "some string":(ver, 0.1+0.2*0.3, qwe.erw);
        var = 10.0/2.5+1.5;
    }
}
